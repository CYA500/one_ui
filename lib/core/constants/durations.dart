// lib/core/constants/durations.dart
class AppDurations {
  AppDurations._();
  static const Duration short = Duration(milliseconds: 150);
  static const Duration medium = Duration(milliseconds: 300);
  static const Duration long = Duration(milliseconds: 500);
  static const Duration clockTick = Duration(milliseconds: 400);
  static const Duration nowBarCycle = Duration(seconds: 5);
  static const Duration breatheCycle = Duration(seconds: 4);
}
